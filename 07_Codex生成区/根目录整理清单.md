# 根目录整理清单

## 1. 说明

本清单用于说明 `D:\AI_workspace\codex` 根目录当前内容的整理结果。  
本轮目标不是清空整个根目录，而是把根目录中的内容分成以下四类：

1. 保留
2. 迁入协作文档库
3. 归档
4. 可删除待确认

同时，已经对“明显散落且不适合继续平铺在根目录”的文件做了收拢处理。

---

## 2. 保留

以下内容当前建议保留在 `D:\AI_workspace\codex` 根目录或其既有结构中：

- `协作文档库`
- `10_知识库导入`
- `tools`
- `package.json`
- `package-lock.json`
- `node_modules`

### 当前理解

- `协作文档库` 是当前 Obsidian + Git 共创主库
- `10_知识库导入` 是原始资料来源库，不适合直接混入协作文档主线
- `tools` + `package.json` + `package-lock.json` + `node_modules` 当前仍属于工具运行环境

---

## 3. 已迁入协作文档库

以下内容已不再留在根目录，而是迁入了 `协作文档库`：

### 3.1 图片与截图

根目录中原本散落的图片截图，已迁入：

- [07_Codex生成区/图片与截图/浏览器与扩展排查](D:/AI_workspace/codex/协作文档库/07_Codex生成区/图片与截图/浏览器与扩展排查)
- [07_Codex生成区/图片与截图/应用商店排查](D:/AI_workspace/codex/协作文档库/07_Codex生成区/图片与截图/应用商店排查)
- [07_Codex生成区/图片与截图/TM弹窗排查](D:/AI_workspace/codex/协作文档库/07_Codex生成区/图片与截图/TM弹窗排查)

### 3.2 结构说明

本轮补充的说明类文档已进入协作文档库：

- [07_Codex生成区/使用说明.md](D:/AI_workspace/codex/协作文档库/07_Codex生成区/使用说明.md)
- [01_共享底座核心层/Git与同步规则.md](D:/AI_workspace/codex/协作文档库/01_共享底座核心层/Git与同步规则.md)

---

## 4. 已归档

以下内容已从根目录收拢，不再继续散落：

### 4.1 历史排查脚本

已迁入：

- `D:\AI_workspace\codex\tools\历史排查脚本`

当前包括：

- `tm_inspect.js`
- `uia_click_extension.ps1`
- `uia_devmode_state.ps1`
- `uia_dump.ps1`
- `uia_navigate_url.ps1`
- `uia_nav_extensions.ps1`
- `uia_open_extensions_from_suggestion.ps1`
- `uia_toggle_devmode.ps1`

### 4.2 历史排查归档

已迁入：

- `D:\AI_workspace\codex\tools\历史排查归档`

当前包括：

- `tmp_chrome_history`
- `tm_backup_20260422_233527`

### 当前理解

这些内容不是当前协作文档主线，但仍有历史排查价值，因此本轮按“归档”处理，而不是直接删除。

---

## 5. 可删除待确认

当前这一类暂时没有被我直接删除的内容。

### 当前理解

本轮我没有直接删除任何根目录内容，原因是：

- `tools` 仍可能复用
- `node_modules` 虽然可以重装，但当前仍与工具环境相关
- 历史排查脚本和归档材料虽然不是主线，但仍可能作为追溯材料

如果后续你明确希望进一步瘦身根目录，下一轮可重点确认：

- `node_modules` 是否还需要长期保留
- `tools\历史排查脚本` 是否仍有复用价值
- `tools\历史排查归档` 是否只需留在 Git 外部备份，不必继续留在当前工作区

---

## 6. 本轮落地结果

本轮完成后，`D:\AI_workspace\codex` 根目录相比之前已经更清晰：

- 不再平铺散落的图片截图
- 不再平铺散落的 UIA / TM 排查脚本
- 不再平铺散落的临时历史排查缓存

根目录当前更接近：

- 协作文档主库
- 原始资料库
- 工具运行区

三者并存但职责分离的状态。
